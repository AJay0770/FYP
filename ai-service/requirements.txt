# Target runtime: Python 3.10 or 3.11.
#
# These pins do NOT all install on Python 3.12+. In particular `deepface` pulls in
# TensorFlow, which publishes no wheels for Python 3.13/3.14 — face recognition
# cannot run on those versions at all. See TRAINING.md > Blockers.
#
# The API service itself (main.py) only needs the first four packages and runs
# fine on newer Python; the rest are for the AI phases.

# --- API service ---
fastapi==0.104.1
uvicorn==0.24.0
python-dotenv==1.0.0

# --- shared ---
numpy==1.24.3
opencv-python==4.8.1.78
scikit-learn==1.3.0

# --- YOLOv8 PPE detection (Phase 7A) ---
torch==2.0.1
torchvision==0.15.2
ultralytics==8.0.0

# --- ArcFace facial recognition (Phase 7B) ---
# Requires TensorFlow; Python 3.10/3.11 only.
deepface==0.0.71

# --- safety_stream.py / dataset tooling ---
requests==2.31.0
pyyaml==6.0.1

# Optional: only needed by scripts/download_dataset.py --source roboflow
# roboflow==1.1.27
